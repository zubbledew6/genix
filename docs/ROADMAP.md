# roadmap

rough list. not a promise.

done enough to use:
- genix-rebuild switch / rollback / generations
- boot snapshots + grub entries (needs btrfs)
- genix-install (stage3, openrc, btrfs)
- live iso build (`iso/build.sh`)
- wifi copy from live → installed
- edit config at install or take bins

still want someday:
- limine backend (i use limine on main pc)
- runit / dinit for services.enable
- prettier real website (someone else is on that)
- fastfetch upstream logo once the site is real
- maybe luks / separate @home in the installer

dont treat this file as a schedule.
